[project]
name = "sudoku"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jan", email = "j.peix04@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pythonturtle>=0.3.2",
]

[project.scripts]
sudoku = "sudoku:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
